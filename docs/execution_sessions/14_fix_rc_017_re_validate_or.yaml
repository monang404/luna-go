session:
  id: SESSION-14
  title: Fix RC-017 — re-validate or document project-context staleness on --resume
  category: architecture
  priority: P2
  complexity: S
  weight: 2
objective: >-
  Either extend the existing project-context staleness check to the --resume path, or explicitly document
  the current behavior (checkpoint reproduces prior session state exactly) as an intentional trade-off
  in CARA-PAKAI.md.
backlog_items:
- id: PROMPT-007
  role: primary
root_causes:
- RC-017
depends_on: []
blocks: []
boundary_rationale: >-
  PROMPT-007 is a standalone architectural decision confined to load_checkpoint.zsh's resume path; the
  acceptance criteria explicitly allow either implementation or documentation as a valid safe end-state.
why_not_more:
- The related, already-accepted design decision ('skills not reloaded on resume') is a separate, already-settled
  trade-off and is not reopened here.
why_not_less:
- The decision (re-validate vs. document) must be made and reflected consistently in both code and docs,
  not left ambiguous.
scope:
  include:
  - 'Decide: re-run staleness check on resume, OR document as intentional trade-off'
  - Implement the chosen outcome
  exclude:
  - The already-settled 'skills not reloaded on resume' design decision
affected_files:
- 50-agent/40-runtime/10-load_checkpoint.zsh
affected_functions:
- _ai_project_context resume path
subtasks:
- id: '01'
  title: Inspect all affected files/functions for re-validate or document project-context staleness on
    --resume
  type: analysis
  required: true
- id: '02'
  title: Implement the recommended fix(es) for this session's backlog items
  type: implementation
  required: true
- id: '03'
  title: Integrate the change with existing callers and verify no other call sites were missed
  type: integration
  required: true
- id: '04'
  title: Verify each acceptance criterion for this session's backlog items
  type: verification
  required: true
- id: '05'
  title: Run regression checks
  type: regression
  required: true
- id: '06'
  title: Update CHANGELOG.md
  type: documentation
  required: true
- id: '07'
  title: Create full repository checkpoint
  type: checkpoint
  required: true
acceptance_criteria:
- id: AC-01
  description: Either freshness is re-validated on resume, or CARA-PAKAI.md explicitly documents this
    as by-design
  verification: STATIC
tests:
  targeted:
  - Resume a checkpoint after simulating a project change (new dependency), confirm decided behavior
regression_checks:
- Normal (non-stale) resume behavior unaffected
risk:
  level: low
  blast_radius: medium
  rollback: easy
completion_state:
  application_usable: true
  syntax_valid: true
  tests_passed: true
  changelog_updated: true
  checkpoint_created: true
checkpoint:
  required: true
  filename: agent-after-SESSION-14.zip
  contains: entire repository
handoff:
  next_session: SESSION-15
  notes: >-
    This session's changes must leave the application fully usable. No downstream session strictly depends
    on this one; safe to run in any order relative to independent sessions.
