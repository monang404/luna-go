session:
  id: SESSION-20
  title: Low-risk CLI naming and convention cleanup batch
  category: cli
  priority: P3
  complexity: S
  weight: 1
objective: >-
  Address three small, independent CLI naming/convention gaps in one batch: document the ai update vs.
  bare update alias distinction, add bare ai*-prefixed functions (or document their absence) for deps/testmodels/research/dev/update,
  and document a consistent stderr-vs-stdout routing convention.
backlog_items:
- id: CLI-003
  role: primary
- id: CLI-005
  role: primary
- id: CLI-006
  role: primary
root_causes: []
depends_on: []
blocks: []
boundary_rationale: >-
  All three are Low-severity, no-shared-root-cause CLI convention gaps with trivial individual scope,
  no data-loss risk, and no overlapping affected files; per the underloaded-session guidance, they are
  grouped into one batch since checkpoint overhead for each individually would dwarf the actual work.
why_not_more:
- CLI-010 (aiundo backup selection) is genuine feature work, not a naming/convention fix, and is scheduled
  as its own session (21) since it carries different acceptance-criteria and testing weight.
why_not_less:
- Each item alone is a one-or-two-line documentation or naming change; combining is the smallest *safe
  coherent* unit given the trivial size.
scope:
  include:
  - Document ai update vs. bare update distinction
  - Add bare ai*-prefixed functions for deps/testmodels/research/dev/update, or document intentional asymmetry
  - Document a stderr/stdout routing convention and note current compliance
  exclude:
  - Any functional behavior change beyond naming/documentation
affected_files:
- 60-ui/40-dispatcher.zsh
- CARA-PAKAI.md
affected_functions: []
subtasks:
- id: '01'
  title: Inspect all affected files/functions for Low-risk CLI naming and convention cleanup batch
  type: analysis
  required: true
- id: '02'
  title: Implement the recommended fix(es) for this session's backlog items
  type: implementation
  required: true
- id: '03'
  title: Integrate the change with existing callers and verify no other call sites were missed
  type: integration
  required: true
- id: '04'
  title: Verify each acceptance criterion for this session's backlog items
  type: verification
  required: true
- id: '05'
  title: Run regression checks
  type: regression
  required: true
- id: '06'
  title: Update CHANGELOG.md
  type: documentation
  required: true
- id: '07'
  title: Create full repository checkpoint
  type: checkpoint
  required: true
acceptance_criteria:
- id: AC-01
  description: Documentation explicitly calls out the ai update vs. bare update distinction
  verification: STATIC
- id: AC-02
  description: Either bare functions added, or explicit documentation of the intentional asymmetry
  verification: STATIC
- id: AC-03
  description: A documented stderr/stdout convention exists
  verification: STATIC
tests:
  targeted:
  - Static review of CARA-PAKAI.md and dispatcher for the three additions
regression_checks:
- No dispatcher routing behavior changes
risk:
  level: low
  blast_radius: low
  rollback: easy
completion_state:
  application_usable: true
  syntax_valid: true
  tests_passed: true
  changelog_updated: true
  checkpoint_created: true
checkpoint:
  required: true
  filename: agent-after-SESSION-20.zip
  contains: entire repository
handoff:
  next_session: SESSION-21
  notes: >-
    This session's changes must leave the application fully usable. No downstream session strictly depends
    on this one; safe to run in any order relative to independent sessions.
