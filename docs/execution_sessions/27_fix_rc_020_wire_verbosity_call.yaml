session:
  id: SESSION-27
  title: Fix RC-020 — wire verbosity call sites to the official getter
  category: technical-debt
  priority: P2
  complexity: S
  weight: 2
objective: >-
  Refactor the 6 files that independently reimplement the ${AI_VERBOSITY:-0} check inline to call the
  existing official _ai_verbose()/_ai_verbose_c() getter instead, consolidating verbosity logic into exactly
  one place.
backlog_items:
- id: UX-005
  role: primary
root_causes:
- RC-020
depends_on: []
blocks: []
boundary_rationale: >-
  UX-005 is a standalone technical-debt refactor confined to the verbosity-checking pattern across 6 named
  files; the feature already works correctly today (confirmed by the audit's own correction), so this
  is a pure consolidation with no behavior-change risk.
why_not_more:
- No other backlog item shares RC-020.
why_not_less:
- All 6 call sites must be refactored together to actually achieve 'exactly one place' per the acceptance
  criterion.
scope:
  include:
  - Refactor 01-logger.zsh, components/state.zsh, 01-chat_display.zsh, 20-tool_step_render.zsh, 25-execute_and_finalize.zsh,
    00-loop_main.zsh to call _ai_verbose()/_ai_verbose_c() instead of inline ${AI_VERBOSITY:-0} checks
  exclude:
  - Any change to verbosity *behavior* — only the call-site implementation changes
affected_files:
- 60-ui/01-logger.zsh
- 60-ui/components/state.zsh
- 60-ui/01-chat_display.zsh
- 20-tool_step_render.zsh
- 25-execute_and_finalize.zsh
- 00-loop_main.zsh
affected_functions:
- _ai_verbose
- _ai_verbose_c
subtasks:
- id: '01'
  title: Inspect all affected files/functions for wire verbosity call sites to the official getter
  type: analysis
  required: true
- id: '02'
  title: Implement the recommended fix(es) for this session's backlog items
  type: implementation
  required: true
- id: '03'
  title: Integrate the change with existing callers and verify no other call sites were missed
  type: integration
  required: true
- id: '04'
  title: Verify each acceptance criterion for this session's backlog items
  type: verification
  required: true
- id: '05'
  title: Run regression checks
  type: regression
  required: true
- id: '06'
  title: Update CHANGELOG.md
  type: documentation
  required: true
- id: '07'
  title: Create full repository checkpoint
  type: checkpoint
  required: true
acceptance_criteria:
- id: AC-01
  description: Verbosity-level logic exists in exactly one place
  verification: STATIC
tests:
  targeted:
  - Run each verbosity level (0/1/2) before and after refactor, confirm byte-identical output
regression_checks:
- All 6 refactored files produce identical output to before the refactor at every verbosity level
risk:
  level: low
  blast_radius: medium
  rollback: easy
completion_state:
  application_usable: true
  syntax_valid: true
  tests_passed: true
  changelog_updated: true
  checkpoint_created: true
checkpoint:
  required: true
  filename: agent-after-SESSION-27.zip
  contains: entire repository
handoff:
  next_session: SESSION-28
  notes: >-
    This session's changes must leave the application fully usable. No downstream session strictly depends
    on this one; safe to run in any order relative to independent sessions.
