session:
  id: SESSION-30
  title: Add plain-listing fallback to Command Palette when gum is unavailable
  category: ui
  priority: P2
  complexity: S
  weight: 1
objective: >-
  Add a plain-listing fallback (e.g. simple echo/fzf list) to ui_palette when gum is not installed, instead
  of hard-failing with an error and losing the Command Palette entirely.
backlog_items:
- id: UX-021
  role: primary
root_causes: []
depends_on: []
blocks: []
boundary_rationale: >-
  UX-021 is a standalone resilience fix confined to the palette's no-gum error path; independent of the
  registry work (Session 22) which addresses the palette's data source, not its dependency-failure behavior.
why_not_more:
- No other backlog item touches this specific fallback path.
why_not_less:
- The fallback must actually list commands (not just avoid the error) to satisfy the acceptance criterion.
scope:
  include:
  - Add plain-listing fallback when gum is unavailable in ui_palette
  exclude:
  - Any change to the gum-available happy path
affected_files:
- 60-ui/screens/palette.zsh
affected_functions:
- ui_palette
subtasks:
- id: '01'
  title: Inspect all affected files/functions for Add plain-listing fallback to Command Palette when gum
    is unavailable
  type: analysis
  required: true
- id: '02'
  title: Implement the recommended fix(es) for this session's backlog items
  type: implementation
  required: true
- id: '03'
  title: Integrate the change with existing callers and verify no other call sites were missed
  type: integration
  required: true
- id: '04'
  title: Verify each acceptance criterion for this session's backlog items
  type: verification
  required: true
- id: '05'
  title: Run regression checks
  type: regression
  required: true
- id: '06'
  title: Update CHANGELOG.md
  type: documentation
  required: true
- id: '07'
  title: Create full repository checkpoint
  type: checkpoint
  required: true
acceptance_criteria:
- id: AC-01
  description: Command Palette remains usable (even if less polished) without gum installed
  verification: MANUAL
tests:
  targeted:
  - Manual test with gum uninstalled/PATH-hidden, confirm plain listing appears instead of hard failure
regression_checks:
- gum-available happy path unaffected
risk:
  level: low
  blast_radius: low
  rollback: easy
completion_state:
  application_usable: true
  syntax_valid: true
  tests_passed: true
  changelog_updated: true
  checkpoint_created: true
checkpoint:
  required: true
  filename: agent-after-SESSION-30.zip
  contains: entire repository
handoff:
  next_session: SESSION-31
  notes: >-
    This session's changes must leave the application fully usable. No downstream session strictly depends
    on this one; safe to run in any order relative to independent sessions.
